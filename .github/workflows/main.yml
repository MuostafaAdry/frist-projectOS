on:
  push:
    branches:
      - master




jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: 'Printing before uses'
        run: ls

      - uses: actions/checkout@v3   
      
      - name: 'Printing after uses'
        run: ls