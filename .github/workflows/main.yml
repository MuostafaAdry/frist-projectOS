on:
  push:
    branches:
      - master




jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3 

      - name: "login to docker hup" 
        run: docker login --username mostafaelsheref --password dckr_pat_TQrC0clGVDl5qbNJ-MFKO5kt9yQ
       
